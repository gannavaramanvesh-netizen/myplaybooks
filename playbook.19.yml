---
- name: Implementing loops
  hosts: all
  tasks:
    - name: install/uninstall/upgrade softwares apps
      apt:
       name: "{{item.a}}"
       state: "{{item.b}}"
       update_cache: "{{item.c}}"
      loop:
         - {a: openjdk-17-jdk,b: absent,c: no}
         - {a: maven,b: absent,c: no}
         - {a: git,b: latest,c: yes}
...
